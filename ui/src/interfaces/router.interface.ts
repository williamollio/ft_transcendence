export enum RoutePath {
  LOGIN = "/login",
  LOGIN_2FA = "/login2fa",
  EDITPROFILE = "/edit-profile",
  PROFILE = "/profile/:user",
  GAME = "/game",
  LOGIN_CALLBACK = "/login/callback",
  REDIRECT = "/redirect",
  SETUP2FA = "/setup2fA",
  STATS = "/statistics",
}
