export enum RoutePath {
  LOGIN = "/login",
  LOGIN_2FA = "/login2fa",
  PROFILE = "/profile",
  GAME = "/game",
  LOGIN_CALLBACK = "/login/callback",
  REDIRECT = "/redirect",
  SETUP2FA = "/setup2FA",
  STATS = "/stats",
}
